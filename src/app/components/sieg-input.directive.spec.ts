import { SiegInputDirective } from './sieg-input.directive';

describe('SiegInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SiegInputDirective();
    expect(directive).toBeTruthy();
  });
});
